/* example 039 – symbolic constants */
#include <stdio.h>

#define PI 3.14
#define TWO_PI (2.0*PI)

int main()
{
    double diameter = 100.0;

    printf("Circumference = %lf\n", diameter*PI);
    printf("2 x PI = %lf\n", TWO_PI);

    return 0;
}
