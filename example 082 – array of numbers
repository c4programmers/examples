/* example 082 – array of numbers */
#include <stdio.h>

int main(void)
{
   int i,j;

   for (i = 1; i <= 10; i++) {
      for (j = 20; j <= 29; j++) {
 printf("%i ",j+i);
      }

      printf("\n");
   }
   return 0;
}
