/* example 157 - check the input buffer */
#include <stdio.h>

int main(void) 
{
   int ch;
  
   while(ch != 'x') {	// Execute the while loop until the character x is entered
      printf("Enter a character:");
      ch = getchar();

      printf("\nYou entered: %c\n", ch);
   }
   return 0;
}
