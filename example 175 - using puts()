/* example 175 - using puts() */
#include <stdio.h>
#include <string.h>

int main( void )
{
    char myString[20];

    strcpy(myString, "Hello World!");

    puts("Output:");
    puts(myString);

    return 0;
}
