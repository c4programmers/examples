/* example 022 - accuracy of float */
#include <stdio.h>

int main(void)
{
   double a, b;
   printf("Enter the first number: ");
   scanf("%lf",&a);

   printf("\nEnter the second number: ");
   scanf("%lf",&b);

   printf("\nThe sum of %lf and %lf = %lf",a,b,a+b);
   return 0;
}
