/* example 224 – advanced macros */
#include <stdio.h>

#define PRINT_NUMBERS(n) \
    for (int i = 0; i < n; i++) { \
        printf("%d ", i); \
    } \
    printf("\n");

int main() 
{
    PRINT_NUMBERS(10); // Generates a for loop that prints numbers 0 to 9
    return 0;
}
