/* example 173 – using sscanf() */
#include <stdio.h>

int main(void)
{
    int d1,d2,d3;
    char c, cBuffer[] = "2022.07.04";

    sscanf(cBuffer, "%d %c %d %c %d", &d1,&c,&d2,&c,&d3);
    printf("Value in cBuffer: %0.2d/%0.2d/%d\n", d3,d2,d1);

    return 0;
}
