/* example 238 – time functions */
#include <stdio.h>
#include <time.h>

int main() 
{
    time_t current_time;
    time(&current_time);
    printf("The current time is %s", ctime(&current_time));
    return 0;
}
